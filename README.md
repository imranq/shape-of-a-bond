# The Shape of a Bond

Interactive 3D molecular orbital visualizer: build any molecule from SMILES, watch bonds form in reactions, and see resonance.

Static site — no build step. Run locally with `npm start` (serves on http://localhost:5173).
